<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

  <application
    android:allowBackup="true"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:requestLegacyExternalStorage="true"
    android:supportsRtl="true"
    android:theme="@style/Theme.RattingMovie"
    android:usesCleartextTraffic="true"
    tools:targetApi="31">
    <activity
      android:name=".BookingTicket.BookingTicketActivity"
      android:exported="false" />
    <activity
      android:name=".QR.QRActivity"
      android:exported="false" />
    <activity
      android:name=".Tiket.TiketActivity"
      android:exported="true" />
    <activity
      android:name=".UserInfo.UserInfoActivity"
      android:exported="true" />
    <activity
      android:name=".MovieDetail.MovieDetailActivity"
      android:exported="true" />
    <activity
      android:name=".MovieList.MovieListActivity"
      android:exported="true">

    </activity>
    <activity
      android:name=".ui.login.RegisterActivity"
      android:exported="false" />
    <activity
      android:name=".WaitingBootProcess.WaitingBootProcessActivity"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity
      android:name=".ui.login.LoginActivity"
      android:exported="false"
      android:label="@string/title_activity_login" />
    <activity
      android:name=".MainActivity"
      android:exported="false" />
  </application>

</manifest>
